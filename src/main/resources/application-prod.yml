spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/user_center?useSSL=false #使用虚拟机模拟远程服务器（没钱）
    username: root
    password: 115474287Zxcczld
  redis:
    host: 127.0.0.1
    port: 6379
    password: 115474287Zxcczld
    database: 0
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 1
        time-between-eviction-runs: 10s

